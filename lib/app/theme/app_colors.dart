import 'package:flutter/material.dart';

abstract class AppColors {
  // Light (Warm Knowledge)
  static const lightCanvas = Color(0xFFF7F3EA);
  static const lightPaper = Color(0xFFFFFCF6);
  static const lightCard = Color(0xFFFFF9F0);
  static const lightElevated = Color(0xFFFFFFFF);
  static const lightMuted = Color(0xFFEFE9DE);
  static const lightTextPrimary = Color(0xFF322F2A);
  static const lightTextSecondary = Color(0xFF6F695F);
  static const lightTextTertiary = Color(0xFF9A9287);
  static const lightBorderSoft = Color(0xFFE4DDD1);

  // Dark
  static const darkCanvas = Color(0xFF181A18);
  static const darkPaper = Color(0xFF1F211F);
  static const darkCard = Color(0xFF252824);
  static const darkElevated = Color(0xFF2B2E2A);
  static const darkMuted = Color(0xFF30332F);
  static const darkTextPrimary = Color(0xFFF1EEE7);
  static const darkTextSecondary = Color(0xFFC4BEB4);
  static const darkTextTertiary = Color(0xFF918B82);
  static const darkBorderSoft = Color(0xFF3B3E39);

  // Accent (shared)
  static const accentSage = Color(0xFF7F9278);
  static const accentSageSoft = Color(0xFFDDE6D8);
  static const accentApricot = Color(0xFFD99A67);
  static const accentApricotSoft = Color(0xFFF4DFCE);
  static const accentBlueMuted = Color(0xFF7B92A8);
  static const accentSageDark = Color(0xFFA4B49C);
  static const accentApricotDark = Color(0xFFE3AE80);

  // Status
  static const success = Color(0xFF6F8D68);
  static const warning = Color(0xFFB98445);
  static const danger = Color(0xFFB7655F);
  static const info = Color(0xFF728CA4);
}
